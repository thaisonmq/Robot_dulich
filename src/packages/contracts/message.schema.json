{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rovera.local/schemas/message.schema.json",
  "title": "RealtimeMessage",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "message_id",
    "schema_version",
    "message_type",
    "robot_id",
    "session_id",
    "sequence",
    "timestamp",
    "ttl_ms",
    "payload"
  ],
  "properties": {
    "message_id": { "type": "string", "format": "uuid" },
    "schema_version": { "const": "1.0" },
    "message_type": {
      "enum": [
        "control.velocity",
        "control.stop",
        "camera.ptz",
        "command.ack",
        "robot.heartbeat",
        "robot.pose",
        "robot.health",
        "navigation.goal",
        "navigation.compute_path",
        "navigation.start",
        "navigation.pause",
        "navigation.resume",
        "navigation.cancel",
        "navigation.speed_mode",
        "navigation.status",
        "navigation.result",
        "navigation.visualization",
        "mapping.start",
        "mapping.stop",
        "mapping.save",
        "mapping.discard",
        "mapping.pause",
        "mapping.resume",
        "mapping.save_draft",
        "mapping.finish",
        "mapping.cancel",
        "mapping.status",
        "map.load",
        "map.deactivate",
        "map.relocalize",
        "map.resync",
        "map.set_initial_pose",
        "map.cache.state",
        "configuration.get",
        "configuration.update",
        "configuration.state",
        "diagnostics.ping",
        "diagnostics.result",
        "media.sources.get",
        "media.sources",
        "media.onvif.scan",
        "media.onvif.devices",
        "media.cameras.get",
        "media.cameras",
        "media.source.select",
        "media.source.state",
        "media.probe",
        "gateway.welcome"
      ]
    },
    "robot_id": { "type": "string", "pattern": "^[A-Z0-9-]{3,64}$" },
    "session_id": { "type": "string", "maxLength": 128 },
    "sequence": { "type": "integer", "minimum": 0 },
    "timestamp": { "type": "string", "format": "date-time" },
    "ttl_ms": { "type": "integer", "minimum": 0, "maximum": 30000 },
    "payload": { "type": "object" }
  }
}
